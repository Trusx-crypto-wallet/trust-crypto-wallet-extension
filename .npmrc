# Ignore peer dependency conflicts on CI
legacy-peer-deps=true

# LavaMoat security configuration
# Disable npm lifecycle scripts by default for security
enable-scripts=false

# Allow specific scripts via @lavamoat/allow-scripts
# This is managed by the "lavamoat" section in package.json
